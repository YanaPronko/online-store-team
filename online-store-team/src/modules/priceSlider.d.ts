type SliderSelectors = {
    sliderRangeSel: string;
    sliderInputSel: string;
    sliderProgressSel: string;
    gap: number;
};
export declare const initFilterSlider: ({ sliderRangeSel, sliderInputSel, sliderProgressSel, gap }: SliderSelectors) => void;
export {};
